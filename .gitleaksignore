# Gitleaks fingerprints to suppress. Format: <commit>:<file>:<rule>:<line>.
#
# Suppress only on a per-finding basis; never wholesale-allowlist a rule.
# Each entry below should have a comment explaining WHY this isn't a real leak.

# Supabase publishable key (sb_publishable_*) — designed to be public per
# Supabase docs (RLS gates data access; the key ships in every browser request).
# The value is also annotated `// gitleaks:allow` inline at web/index.html:144;
# this fingerprint suppresses the historical commit that introduced the key
# before the inline annotation was added.
7a8d0df23c658d071472da799776349908e458cc:web/index.html:generic-api-key:144
