What I liked - Remind's bottom right corner could be a chat icon so the user can always access the AI from any screen. The Public OS is a pretty smart idea to allow for multiple options - let's say, you press and hold and it opens up the menu that expands to 3 total icons to allow you to select something like, 'somatic reset' and 'today's log' (for things you wanted to work on today) - emergency access buttons, essentially. But the default 'chat' is always there by default, and you just tap it to trigger it. 