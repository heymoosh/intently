{
  "name": "intently-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Root tooling package — holds the build-watchdog ESLint config that covers web/*.jsx (the deployed prototype). The Expo/RN-Web app has its own package at app/package.json (kept as historical reference per ADR 0004).",
  "scripts": {
    "lint": "eslint \"web/**/*.jsx\" --max-warnings=0"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "eslint-plugin-react": "^7.37.4",
    "globals": "^15.14.0"
  }
}
