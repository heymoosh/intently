---
captured: 2026-04-25T22:00:00-05:00
session: chat/0425-210554
source: discussion
handoff: .claude/handoffs/wiring-audit.md
---

# Wiring audit — exhaustive interaction inventory + decisions

**Handoff drafted at `.claude/handoffs/wiring-audit.md`.** `/groom` should add a TRACKER § Active handoffs row, register AC inside the handoff, update this `resolved:` field, then delete this file.

## One-line intent

Catalog every interactive element in the deployed app, decide its wiring story (ship / defer / reject), and stop discovering gaps ad-hoc. Build the artifact that should have existed during the cognition push.

## Why this is in the inbox

Muxin's retrospective question: *"is that something we didn't do properly because we weren't grooming, or what?"* — yes, the wiring sprint scoped to the demo path (per `web/WIRING-POINTS.md` and `launch-plan.md`'s explicit non-requirements list), no exhaustive interaction enumeration ever happened. Every gap surfacing now (avatar inconsistency, edit-button stub, brief-can't-open-in-reading-mode, todo checkboxes don't persist, etc.) is a member of a category that was never enumerated. Audit the whole surface area, decide per-item, fold into the right destination.

## Suggested execution order across all current handoffs

This handoff should run **first** — its output sequences the others. After the audit:

1. Items routing to `new-user-ux-and-auth` (avatar, identity strings, setup expansion) — fold into that handoff's AC
2. Items routing to `oauth-calendar-email` — fold there
3. Items routing to `scheduled-agent-dispatch` — fold there
4. Items routing to `cognition-verification-harness` — fold there as test coverage targets
5. Standalone items (e.g. "wire the JournalReader edit button," "fix brief-kind reading-mode resolver") — TRACKER § Next directly
6. Defer items — post-launch backlog rows in TRACKER

See handoff for full AC, sample inventory table, tooling proposals (lint rule, PR-template checkbox).
