---
captured: 2026-04-25T19:00:00-04:00
session: chat/0425-171303
source: discussion
---

# Add inbox+grooming pointer to TRACKER doc hierarchy

Phase 2. Add a row to TRACKER's "Current state pointers" table for the capture-groom-execute model. Or extend the doc hierarchy on line 5 of TRACKER.md:

```
launch-plan.md (strategy) → TRACKER.md (queue, hot) → .claude/inbox/ (raw captures, ungroomed) → .claude/handoffs/<slug>.md (project depth) → docs/product/acceptance-criteria/ (done-definition, per skill)
```

Inbox slots between TRACKER and handoffs as the "pre-queue" layer. This row formalizes that architecture.
