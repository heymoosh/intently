---
captured: 2026-04-25T19:00:00-04:00
session: chat/0425-171303
source: discussion
---

# CLAUDE.md one-line pointer for capture/groom/execute

Phase 2. Add a single line to CLAUDE.md (within 100-line cap) pointing at the model. Probable home: near the existing "Where everything else lives" section. One-liner like:

`**Capture mid-discussion** with /capture or auto-drop into .claude/inbox/; groom via /groom (writes TRACKER + AC); execute via /work-next (refuses ungroomed items).`

Keep it brief — full spec lives in `.claude/handoffs/capture-groom-execute.md`.
