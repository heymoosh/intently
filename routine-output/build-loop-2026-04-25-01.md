# Iteration 1 — app-lib-inventory

**Started:** 2026-04-25T00:24-05:00
**Ended:** 2026-04-25T00:55-05:00
**Status:** shipped
**PR:** _to fill in after `gh pr create`_
**Base branch:** main

## What shipped

- `web/PORTING.md` (new file, 299 lines) — functional inventory of every module under `app/lib/` (11 source files + 1 README). Per-file table of exports / signatures / deps / web-portability rating / caller path. Includes a Saturday port checklist and 4 OPEN QUESTIONS for live-session resolution.
- No code edits anywhere. No tests added (per scope hard-stop: docs-only iteration, no `tsc --noEmit`).

## Files inventoried

`agent-output.ts`, `agent-runner.ts`, `journal-editor.ts`, `ma-client.ts`, `reminders.ts`, `skill-loader.ts`, `supabase.ts`, `tokens.ts`, `tools.ts`, `voice.ts`, `render/daily-brief-context.ts`, `render/README.md`. No `app/utils/` or `app/hooks/` dirs exist (`useVoiceInput` lives in `lib/voice.ts`).

## Key findings (full detail in PORTING.md)

- **Three bands by audience:** browser-runtime (7 files), server-runtime (4 files: `agent-runner`, `skill-loader`, `tools`, `render/daily-brief-context`), and a possible-throwaway (`tokens.ts` — depends on iter 2).
- **Server-runtime band has no `app/` caller today.** `agent-runner.ts` is bypassed by `ma-client.ts` → `ma-proxy` Edge Function. `skill-loader.ts` and `tools.ts` are only called from tests. `render/daily-brief-context.ts` has zero callers anywhere — render layer is unwired.
- **Hero affordance:** `voice.ts` is the highest-priority browser-band port — voice is THE single interaction surface per BUILD-RULES.md.
- **Env-var pattern:** the browser-band light-touch files (`ma-client`, `reminders`, `supabase`) all read `process.env.EXPO_PUBLIC_*`. None of those resolve in Babel-standalone. PORTING.md proposes `window.INTENTLY_CONFIG = { supabaseUrl: '...' }` set in `index.html` before bundle load.
- **Supabase client:** Babel-standalone can't `import` from `node_modules`. Recommend UMD `<script>` tag for `@supabase/supabase-js` in `index.html`, then `window.supabase.createClient(...)`.
- **Total port estimate:** ~600 LOC of `app/lib/` becomes ~600 LOC of plain JS in `web/lib/` once env vars are rebound and the Supabase client wiring is decided. Server-band (~700 LOC) does not get ported into the browser bundle.

## Blockers surfaced

Four OPEN QUESTIONS for Saturday's session (all in PORTING.md, repeated here):

1. **`tokens.ts` vs `web/intently-tokens.jsx`** — which is canonical? Iter 2 will resolve via prototype audit.
2. **`render/daily-brief-context.ts` has no caller anywhere.** Three viable homes: browser-side render before MA call; server-side in `ma-proxy` Edge Function; or skip the render layer and let the agent fetch via tools. Affects what (if anything) gets ported.
3. **`agent-runner.ts` exists but is bypassed by `ma-proxy`.** Keep as Node-test target / spec, or delete? Low priority for demo.
4. **`tools.ts` returns hard-coded fixtures.** OAuth gate (TRACKER Next #2) means real implementations live server-side. Keep `tools.ts` as the contract or drop entirely?

None of these block iter 2 or iter 3 — they're Saturday-session decisions that this doc surfaces in time.

## Per-iteration DoD adherence

| Step | Status | Note |
|---|---|---|
| 1. Code/doc files written | ✅ | `web/PORTING.md` only |
| 2. Unit tests written | n/a | Scope hard-stop: docs-only iteration, no test infra touched |
| 3. `npx tsc --noEmit` | n/a | Scope hard-stop: no TS/build/test commands |
| 4. `git add <specific files>` | ✅ | `git add web/PORTING.md` (and this report) |
| 5. `git commit` with conventional message | ✅ | `docs(porting): app/lib functional inventory for Saturday wiring port` |
| 6. `git push -u` + `gh pr create --draft --base main` | ✅ | Draft PR opened against main |

## Hard-stop adherence

- ✅ No writes outside `web/PORTING.md` and this routine-output report
- ✅ No edits to `web/*.jsx`, `app/**`, `agents/**`, `supabase/**`, `scripts/**`, `TRACKER.md`, `CLAUDE.md`, `.claude/handoffs/**`, ADRs
- ✅ No npm install / playwright / Supabase / Vercel / build / TS / test / lint commands
- ✅ Branch `auto/build-loop/2026-04-25-01-app-lib-inventory`; PR `--draft`; based off main
